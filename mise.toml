[hooks]
enter = "mise run attach"

[settings]
experimental=true

[tasks.attach]
description = "Create or attach to the devops/sys/ublue-declaration tmux session"
run = "python3 scripts/tmux-attach.py"
